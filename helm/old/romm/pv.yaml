# This is how PV looked with smb, before moving to NFS

# This volume requires smb csi drivers - https://github.com/kubernetes-csi/csi-driver-smb
apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ printf "%s-games" .Release.Name | quote }}
  labels:
    {{- include "common.labels" . | nindent 4 }}
    app.kubernetes.io/component: storage
  annotations:
    pv.kubernetes.io/provisioned-by: smb.csi.k8s.io
spec:
  capacity:
    storage: 100Gi
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  mountOptions:
    - file_mode=0664
    - dir_mode=02775
    - uid={{ printf "%d" (int64 .Values.app.uid) }}
    - gid={{ printf "%d" (int64 .Values.app.gid) }}
    - noperm
    - noserverino
  csi:
    driver: smb.csi.k8s.io
    volumeHandle: {{ printf "%s-smb-share-01" .Release.Name | quote }}
    volumeAttributes:
      source: {{ printf "//%s/%s" .Values.storage.smb.url .Values.storage.smb.share | quote }}
    nodeStageSecretRef:
      name: {{ printf "%s-smb-credentials" .Release.Name | quote }}
      namespace: kube-system