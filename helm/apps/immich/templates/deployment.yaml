{{ include "baseResources.service" (dict "root" $ "vals" .Values.app) }}
---
{{ include "baseResources.httproute" (dict "root" $ "vals" .Values.app) }}
---
{{ include "redis.resources" (dict "root" $ "vals" .Values.redis) }}
---
{{ include "cnpg.resources" (dict "root" $ "vals" .Values.mainDb) }}
---
{{- with .Values.app }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ printf "%s-server" $.Release.Name | quote }}
  labels:
    {{- include "common.labels" $ | nindent 4 }}
    app.kubernetes.io/component: {{ .component | quote }}
  annotations:
    reloader.stakater.com/auto: "true"

spec:
  revisionHistoryLimit: 3
  replicas: {{ .replicaCount | default 1 }}
  selector:
    matchLabels:
      app.kubernetes.io/instance: {{ $.Release.Name | quote }}
      app.kubernetes.io/component: {{ .component | quote }}
  template:
    metadata:
      labels:
        {{- include "common.labels" $ | nindent 8 }}
        app.kubernetes.io/component: {{ .component | quote }}
    spec:
      automountServiceAccountToken: false
      initContainers:
        {{- include "baseResources.waitForPostgres" $.Values.mainDb | nindent 8 }}
        {{- include "baseResources.waitForNfs" (dict "volumeName" "library") | nindent 8 }}
        {{- include "baseResources.waitForRedis" (dict
            "host" (include "redis.serviceFQDN" (dict "root" $))
            "password" $.Values.redis.password) | nindent 8 }}

      containers:
        {{- include "baseResources.baseContainer" (dict "root" $ "vals" .) | indent 8 }}

          envFrom:
            - configMapRef:
                name: {{ printf "%s-env" $.Release.Name | quote }}
                optional: false
            - secretRef:
                name: {{ printf "%s-secret" $.Release.Name | quote }}
                optional: false

          volumeMounts:
            - name: library
              mountPath: /data

      volumes:
        - name: library
          nfs:
            server: {{ $.Values.storage.nfs.server | quote }}
            path: {{ $.Values.storage.nfs.path | quote }}
{{- end }}
---

# MachineLearning
{{ include "baseResources.service" (dict "root" $ "vals" .Values.machineLearning) }}
---
{{- with .Values.machineLearning }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ printf "%s-ml" $.Release.Name | quote }}
  labels:
    {{- include "common.labels" $ | nindent 4 }}
    app.kubernetes.io/component: {{ .component | quote }}
  annotations:
    reloader.stakater.com/auto: "true"

spec:
  revisionHistoryLimit: 3
  replicas: {{ .replicaCount | default 1 }}
  selector:
    matchLabels:
      app.kubernetes.io/instance: {{ $.Release.Name | quote }}
      app.kubernetes.io/component: {{ .component | quote }}

  strategy:
    type: Recreate

  template:
    metadata:
      labels:
        {{- include "common.labels" $ | nindent 8 }}
        app.kubernetes.io/component: {{ .component | quote }}

    spec:
      automountServiceAccountToken: false

      securityContext:
        supplementalGroups: [105]

      containers:
        {{- include "baseResources.baseContainer" (dict "root" $ "vals" .) | indent 8 }}

          resources:
            limits:
              gpu.intel.com/i915: 1

          volumeMounts:
            - name: model-cache
              mountPath: /cache

      volumes:
        - name: model-cache
          persistentVolumeClaim:
            claimName: {{ printf "%s-ml-model-cache" $.Release.Name | quote }}
{{- end }}