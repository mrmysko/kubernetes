apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ printf "%s-files" .Release.Name | quote }}
  labels:
    {{- include "common.labels" . | nindent 4 }}
    app.kubernetes.io/component: storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
{{- with .Values.storage.files }}
  volumeName: {{ .volumeName | quote }}
{{- end }}