apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ printf "%s-config-pvc" .Release.Name | quote }}
  labels:
    {{- include "common.labels" . | nindent 4 }}
    app.kubernetes.io/component: storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
{{- with .Values.storage.config }}
  volumeName: {{ .volumeName | quote }}
{{- end }}