apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ printf "%s-configmap" .Release.Name | quote }}
  labels:
    {{- include "common.labels" . | nindent 4 }}
  annotations:
    reloader.stakater.com/auto: "true"
data:
  navidrome.toml: |
    LogLevel = 'warn'
    EnforceNonRootUser = 'true'
    EnableInsightsCollector = 'false'
    BaseUrl = 'https://{{ .Values.app.route.baseUrl }}'
    ExtAuth.TrustedSources = '{{ .Values.config.trustedSources }}'
    ExtAuth.UserHeader = 'Remote-User'
    ExtAuth.LogoutURL = '{{ .Values.config.logoutUrl }}'

    LastFM.Enabled = 'true'
    Deezer.Enabled = 'true'
    ListenBrainz.Enabled = 'false'
    Agents = 'audiomuseai,deezer,lastfm'
    LastFM.Language = 'en'

    EnableCoverAnimation = 'true'
    EnableSharing = 'false'
    DefaultTheme = 'nord'
    AutoImportPlaylists = 'true'
    SessionTimeout = '48h'
    EnableStarRating = 'false'
    Scanner.Enabled = 'true'
    Scanner.WatcherWait = '5s'
    Scanner.ScanOnStartup = 'true'
    EnableWebPEncoding = 'true'
    Plugins.Enabled = 'true'