{{ include "baseResources.service" (dict "root" $ "vals" .Values.app) }}
---
{{ include "baseResources.httproute" (dict "root" $ "vals" .Values.app) }}
---
{{- with .Values.app }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ $.Release.Name | quote }}
  labels:
    {{- include "common.labels" $ | nindent 4 }}
    app.kubernetes.io/component: {{ .component | quote }}
  annotations:
    reloader.stakater.com/auto: "true"
spec:
  revisionHistoryLimit: 3
  replicas: {{ .replicaCount | default 1 }}
  strategy:
    type: Recreate

  selector:
    matchLabels:
      app.kubernetes.io/instance: {{ $.Release.Name | quote }}
      app.kubernetes.io/component: {{ .component | quote }}

  template:
    metadata:
      labels:
        {{- include "common.labels" $ | nindent 8 }}
        app.kubernetes.io/component: {{ .component | quote }}
    spec:
      automountServiceAccountToken: false
      securityContext:
        fsGroup: {{ .gid }}

      initContainers:
        {{- include "baseResources.waitForSamba" $.Values.storage.smb | nindent 8 }}

      containers:
        {{- include "baseResources.baseContainer" (dict "root" $ "vals" .) | indent 8 }}

          envFrom:
            - secretRef:
                name: {{ printf "%s-secret" $.Release.Name | quote }}
          env:
            - name: ND_CONFIGFILE
              value: /data/navidrome.toml

          volumeMounts:
            - name: config-pvc
              mountPath: /data
            - name: config
              mountPath: /data/navidrome.toml
              subPath: navidrome.toml
              readOnly: true
            - name: music
              mountPath: /music
              readOnly: true
            - name: cache
              mountPath: /data/cache

      volumes:
        - name: config-pvc
          persistentVolumeClaim:
            claimName: {{ printf "%s-config-pvc" $.Release.Name | quote }}
        - name: config
          configMap:
            name: {{ printf "%s-configmap" $.Release.Name | quote }}
            items:
              - key: navidrome.toml
                path: navidrome.toml
        - name: music
          persistentVolumeClaim:
            claimName: {{ printf "%s-music" $.Release.Name | quote }}
        - name: cache
          emptyDir:
            medium: Memory
{{- end }}