apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaContactPoint
metadata:
  name: {{ printf "%s-contactpoint" .Release.Name | quote }}
spec:
  name: SMTP
  instanceSelector:
    matchLabels:
      dashboards: grafana
  # Editing ContactPoints in the UI is disabled by default
  editable: true # editable is immutable and can only be set at creation
  receivers:
    - type: email
      settings:
        subject: Grafana Alert
      valuesFrom:
        - targetPath: addresses
          valueFrom:
            secretKeyRef:
              name: {{ printf "%s-contactpoint-secret" .Release.Name | quote }}
              key: alert-mails