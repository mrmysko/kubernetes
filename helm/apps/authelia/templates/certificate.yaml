{{- $fullname := include "baseResources.fullname" (dict "root" $ "vals" .Values.app ) }}

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ printf "%s-server-cert" .Release.Name }}
  labels:
    {{- include "common.labels" . | nindent 4 }}
spec:
  secretName: {{ printf "%s-server-cert" .Release.Name }}
  issuerRef:
    name: cluster-ca
    kind: ClusterIssuer
  privateKey:
    rotationPolicy: Always
  commonName: "{{ $fullname }}-svc.{{ .Release.Namespace }}.svc.cluster.local"
  dnsNames:
    - "{{ $fullname }}-svc.{{ .Release.Namespace }}.svc.cluster.local"
    - "{{ $fullname }}-svc.{{ .Release.Namespace }}.svc"
    - "{{ $fullname }}-svc.{{ .Release.Namespace }}"
    - "{{ $fullname }}-svc"
    - "localhost"
