apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ print .Release.Name "-server-cert" | quote }}
  labels:
    {{- include "common.labels" . | nindent 4 }}
spec:
  secretName: {{ print .Release.Name "-server-cert" | quote }}
  issuerRef:
    name: cluster-ca
    kind: ClusterIssuer
  privateKey:
    rotationPolicy: Always
  commonName: "{{ .Release.Name }}-svc.{{ .Release.Namespace }}.svc.cluster.local"
  dnsNames:
    - "{{ .Release.Name }}-svc.{{ .Release.Namespace }}.svc.cluster.local"
    - "{{ .Release.Name }}-svc.{{ .Release.Namespace }}.svc"
    - "{{ .Release.Name }}-svc.{{ .Release.Namespace }}"
    - "{{ .Release.Name }}-svc"
    - "localhost"
